// Exercise 18:
// Declare an interface called Incrementable which represents items that store an integer
// that can be incremented in some way. The interface has one method called increment
// that increments the value and a method called getValue that returns the value.

public interface Incrementable {
    public void increment();

    public int getValue();
}